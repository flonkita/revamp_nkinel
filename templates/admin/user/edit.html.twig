{% extends 'admin/base.html.twig' %}

{% block title %}User
{% endblock %}

{% block content %}
	<div class="container-fluid">
	<div class="container">
	<div class="container-fluid p-3  w-100 mx-auto bg-white border rounded">
	<h1 class="h3 mb-0 text-gray-800">Les Utilisateurs</h1>

			<p class="mb-2">Modifier mon utilisateur</p>
            
           {{ form_start(formUser) }}
            <div class ="row g-3">

            <div class="col-6">
        	{{form_row(formUser.prenom)}}
			</div>

            <div class="col-6">
        	{{form_row(formUser.email)}}
			</div>

           </div>
           {{ form_end(formUser) }}

          
	</div>
	</div>
	</div>

{% endblock %}